#include file_utils.h

int main(char* input_filename, char* output_filename){
	//create variable to fill with file contents
	////indicate to read_file which file and a place to store contents
	//reads the file, returns # of bytes read
	//
	////reversie hint do a loopy loop?
	//
	//take that reversie and write it to a new place  
}
